services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: talent_matching_db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-talent}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-talent_dev}
      POSTGRES_DB: ${POSTGRES_DB:-talent_matching}
    command: postgres -c max_connections=300
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-talent} -d ${POSTGRES_DB:-talent_matching}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    mem_limit: 3g
    cpus: 2
    networks:
      - dagster_network

  dagster-code:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dagster_code
    entrypoint: ["/entrypoint.sh"]
    command:
      - dagster
      - api
      - grpc
      - -h
      - "0.0.0.0"
      - -p
      - "4266"
      - -m
      - talent_matching.definitions
    env_file:
      - .env
    environment:
      POSTGRES_HOST: postgres
      ENVIRONMENT: production
      DAGSTER_HOME: /opt/dagster/dagster_home
    volumes:
      - dagster_home:/opt/dagster/dagster_home
      - ./docker/dagster.yaml:/opt/dagster/dagster_home/dagster.yaml:ro
      - /data/dagster_storage:/opt/dagster/dagster_home/storage
    ports:
      - "127.0.0.1:4266:4266"
    healthcheck:
      test: ["CMD", "python", "-c", "import socket; s=socket.socket(); s.settimeout(2); s.connect(('127.0.0.1',4266)); s.close()"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 30s
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    mem_limit: 9g
    cpus: 5
    networks:
      - dagster_network

  dagster-daemon:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dagster_daemon
    entrypoint: ["/entrypoint.sh"]
    command:
      - dagster-daemon
      - run
      - -w
      - /opt/dagster/workspace.yaml
    env_file:
      - .env
    environment:
      POSTGRES_HOST: postgres
      ENVIRONMENT: production
      DAGSTER_HOME: /opt/dagster/dagster_home
    volumes:
      - dagster_home:/opt/dagster/dagster_home
      - ./docker/dagster.yaml:/opt/dagster/dagster_home/dagster.yaml:ro
      - ./docker/workspace.yaml:/opt/dagster/workspace.yaml:ro
      - /data/dagster_storage:/opt/dagster/dagster_home/storage
    depends_on:
      postgres:
        condition: service_healthy
      dagster-code:
        condition: service_healthy
    restart: unless-stopped
    mem_limit: 2g
    cpus: 1
    networks:
      - dagster_network

volumes:
  postgres_data:
  dagster_home:

networks:
  dagster_network:
    driver: bridge
